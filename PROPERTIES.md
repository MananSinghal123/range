# Properties

> Generated by the Fizz skill. Each property is described in English and has one
> of three states:
> - `[ ]` pending — not yet implemented; `/fizz-convert` will pick it up
> - `[x]` implemented — Solidity exists in Properties.sol / handlers
> - `[-]` skipped / manual — `/fizz-convert` will NOT touch it. Use this for TODO stubs,
>   properties that need human judgment, or properties you add yourself later.
>
> Users may freely add their own entries (use any unused `GL-NN` / `SP-NN` ID) and mark
> them `[ ]` to have `/fizz-convert` implement them, or `[-]` to leave them as a spec
> note only.
>
> IDs are stable. Do not renumber.
>
> **Guarantee tag.** Every property is tagged `SHOULD-HOLD` or `EXPLORATORY`:
> - `SHOULD-HOLD` — explicitly guaranteed by docs/spec/standard or an exact identity
>   (evidence cited inline). If the campaign violates it, that is a **confirmed bug**.
> - `EXPLORATORY` — inferred, not explicitly promised. A violation is a **lead flagged
>   for human review**, not an automatic bug.
>
> This tag is set at generation time so violations can be triaged without post-hoc
> severity guessing. If you edit a property's logic such that it is no longer guaranteed,
> change its tag to `EXPLORATORY`.
>
> **Harness note.** This vault is NOT a faithful ERC-4626: `redeem` pays out BOTH token0
> and token1 in-kind (not just the asset), and `withdraw` can legitimately revert on a
> token0 shortfall (`InsufficientToken0ForWithdraw`) even when `assets <= maxWithdraw`.
> ERC-4626-by-analogy clauses that aren't literally honored by this vault are tagged
> `EXPLORATORY`, not `SHOULD-HOLD`.
>
> **Ghost dependency.** `lastDepositBlock` is a PRIVATE mapping (no getter). Properties
> that need it (GL-18 partially, SP-12, SP-19, SP-20) rely on a ghost mirror
> `ghost_lastDepositBlock[address]` maintained by the harness and updated in the
> deposit/mint/depositToken1 handlers to shadow the private state.

## Global Properties
> Always-on invariants. Implemented as `public function property_*()` in `test/fizz/Properties.sol`.

- [ ] **GL-01** — `totalSupply()` equals the sum of `balanceOf(h)` over the enumerable holder set `{0xdead, admin, actors[0..2]}` — no phantom mint/burn outside enumerated holders. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — OZ ERC20 `_mint`/`_burn`/`_transfer` update `_totalSupply` and `_balances` atomically; vault only mints in deposit/mint/depositToken1 (:397,410,436-437,655-656,668) and burns in withdraw/redeem (:491,591); setup guarantees the holder set is exactly the 5 enumerated addresses; Priority: HIGH; Sources: CON-01)
- [ ] **GL-02** — Once `totalSupply() > 0`, `balanceOf(0xdead) == DEAD_SHARES (1000)` forever. (Category: VALID_STATE; Guarantee: SHOULD-HOLD — `DEAD_SHARES` minted once, gated by `supply==0` (:395-397,436,653-656); no burn/transfer path from `0xdead` exists in the contract; Priority: HIGH; Sources: CON-02)
- [ ] **GL-03** — `token0.balanceOf(vault)` (idle token0) is always `<= totalAssets()`. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — `_totalVaultValueInToken0` (:1048-1086) starts from `bal0 = balanceOf(this)` and only adds non-negative position value + owed fees + token1-leg value; Priority: LOW; Sources: CON-03)
- [ ] **GL-04** — `totalFees0Earned()` and `totalFees1Earned()` are monotonically non-decreasing across all calls. (Category: VARIABLE_TRANSITION; Guarantee: SHOULD-HOLD — the only write sites are `+=` (:486-487,563-564,759-760,857-858) and fee amounts are always `>= 0`; Priority: MEDIUM; Sources: CON-04, VT-02)
- [ ] **GL-05** — `convertToAssets(convertToShares(x)) <= x` and `convertToShares(convertToAssets(s)) <= s` (floor∘floor composition is never inflationary). (Category: VARIABLE_TRANSITION; Guarantee: SHOULD-HOLD — both `convertToShares`/`convertToAssets` use `Math.mulDiv(..., Math.Rounding.Floor)` (:296-319); Priority: HIGH; Sources: CON-10, RD-03, SPEC-05)
- [ ] **GL-06** — `rebalance()` never reverts with an arithmetic underflow panic (`0x11`) while computing `feesOwed0`/`feesOwed1`. (Category: HIGH_LEVEL; Guarantee: EXPLORATORY — the `uint128` subtraction at `_rebalanceRemoveFeeCollectBurn` (:840-841) is unguarded and its safety depends on mock/pool invariants (`tokensOwed >= principal`) that are not independently proven; Priority: MEDIUM; Sources: CON-12, ADV-18)
- [ ] **GL-07** — `token0.balanceOf(feeRecipient) == totalFees0Earned()` and `token1.balanceOf(feeRecipient) == totalFees1Earned()` (feeRecipient `0xFEE` receives value only via `_deductPerformanceFee`). (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — `_deductPerformanceFee` (:1181-1202) is the only transfer path to `feeRecipient`, and each transfer is immediately followed by the matching `totalFeesXEarned +=`; the harness never pranks `feeRecipient` or donates to it; Priority: HIGH; Sources: CON-13)
- [ ] **GL-08** — `totalSupply() > DEAD_SHARES` implies `totalAssets() > 0`. (Category: HIGH_LEVEL; Guarantee: EXPLORATORY — plausible from the deposit/mint guards but no exact cited identity; Priority: LOW; Sources: CON-14)
- [ ] **GL-09** — `initializePosition` reverts with `AlreadyInitialized` whenever `tokenId() != 0`. (Category: VALID_STATE — guards the "position already initialized" state; Guarantee: SHOULD-HOLD — explicit guard `if (s.tokenId != 0) revert AlreadyInitialized()` (:681); Priority: HIGH; Sources: ST-01)
- [ ] **GL-10** — `applyPerformanceFee()` reverts with `TimelockActive` whenever `block.timestamp < feeChangeActiveAt()`. (Category: VALID_STATE — guards the fee-timelock state; Guarantee: SHOULD-HOLD — explicit guard (:978-984); Priority: HIGH; Sources: ST-05)
- [ ] **GL-11** — When `paused() == true`, every one of `deposit`/`mint`/`depositToken1`/`withdraw`/`redeem`/`rebalance`/`collectFees` reverts. (Category: VALID_STATE — guards the paused mode; Guarantee: SHOULD-HOLD — `whenNotPaused` modifier (:150-153) is applied to all seven entry points; Priority: HIGH; Sources: ST-09)
- [ ] **GL-12** — `rebalanceCount()` never decreases, and only ever advances by exactly 1 per successful `rebalance()` call. (Category: VARIABLE_TRANSITION — tracks `rebalanceCount`; Guarantee: SHOULD-HOLD — single write site `s.rebalanceCount++` (:907); Priority: HIGH; Sources: VT-01)
- [ ] **GL-13** — Once `tokenId()` becomes nonzero, it never returns to 0. (Category: VARIABLE_TRANSITION — tracks `tokenId`; Guarantee: SHOULD-HOLD — the only write sites are the guarded initial set (:702) and the fresh nonzero rebalance mint (:906); no zero-assignment exists; Priority: HIGH; Sources: VT-03)
- [ ] **GL-14** — `performanceFeeBps()` is always `<= 1000` (10%). (Category: VARIABLE_TRANSITION — tracks `performanceFeeBps`; Guarantee: SHOULD-HOLD — `proposePerformanceFee` guard `if (bps > 1000) revert FeeTooHigh()` (:969), `applyPerformanceFee` only ever copies the already-bounded `pendingFeeBps` (:981), and init sets it to 1000; Priority: MEDIUM; Sources: VT-04)
- [ ] **GL-15** — `slippageBps() <= 500`, `twapSeconds() >= 60`, and `maxTwapDeviationTicks()` is in `(0, 1000]` at all times. (Category: VARIABLE_TRANSITION — tracks the three risk-parameter variables; Guarantee: SHOULD-HOLD — setter guards at `:1007` (slippage), `:997` (twap seconds), `:1002` (deviation ticks); Priority: MEDIUM; Sources: VT-05)
- [ ] **GL-16** — `totalSupply() == 0` iff the vault has never received a deposit; after the first successful deposit, `totalSupply() >= DEAD_SHARES` forever. (Category: VALID_STATE — guards the "pristine vs seeded" state; Guarantee: SHOULD-HOLD — dead-share mint gated by `supply==0` (:395-398,653-656), no burn of `0xdead` exists; Priority: HIGH; Sources: VS-02)
- [ ] **GL-17** — A solvent, non-same-block holder can always call `redeem(maxRedeem(holder), ...)` without reverting (unless paused). (Category: HIGH_LEVEL — liveness; Guarantee: EXPLORATORY — inferred from the vault's own `maxRedeem`/`redeem` contract, not an externally documented guarantee; requires the `ghost_lastDepositBlock` mirror to select a non-same-block holder; Priority: HIGH; Sources: ADV-01)
- [ ] **GL-18** — A solvent, non-same-block holder can always call `withdraw(maxWithdraw(holder), ...)` without reverting, even when a token1→token0 shortfall swap is required — the only acceptable reverts are documented ones (e.g. `InsufficientToken0ForWithdraw`, `SameBlock`), never an `ExceedsMax*` or panic. (Category: HIGH_LEVEL — liveness; Guarantee: EXPLORATORY — `maxWithdraw` is an optimistic ceiling and the shortfall-swap path can legitimately revert (:514), so this probes for *unexpected* reverts rather than a hard guarantee; requires the `ghost_lastDepositBlock` mirror; Priority: HIGH; Sources: ADV-02, SPEC-04)
- [ ] **GL-19** — Per-actor, cumulative value extracted from the vault never exceeds cumulative value put in plus a small epsilon (no free profit, including from donations — donated value is NOT credited to `ghost_valueIn`). (Category: HIGH_LEVEL; Guarantee: EXPLORATORY — inferred economic guarantee, not a cited identity; needs `ghost_valueIn`/`ghost_valueOut` per actor priced at TWAP; Priority: HIGH; Sources: ADV-03, ADV-16)
- [ ] **GL-20** — `previewDeposit(1e18) > 0` and `previewMint(1) != type(uint256).max` at all times once `totalSupply() > 0`, regardless of prior donations (first-depositor / donation-attack inflation resistance). (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — `DEAD_SHARES` anti-inflation mechanism (:395-398); `previewMint` only returns the `type(uint256).max` sentinel when `totalAssets()==0` with `supply>0` (:339-348), which this property probes never occurs; Priority: HIGH; Sources: ADV-06, SPEC-02, ADV-07 — **MANDATORY (V-06), non-mergeable, do not drop**)
- [ ] **GL-21** — A non-operator address can never successfully call `rebalance()` or `collectFees()`. (Category: STATE_TRANSITION — illegal-caller transition must revert; Guarantee: SHOULD-HOLD — `onlyOperator` modifier (:145-148) applied at `:713,:768`; Priority: HIGH; Sources: ADV-11)
- [ ] **GL-22** — A non-owner address can never successfully call any owner-gated setter (`setOperator`, `setGuardian`, `setStrategy`, `setDexAdapter`, `setPaused`, `proposePerformanceFee`, `applyPerformanceFee`, `setTwapSeconds`, `setMaxTwapDeviationTicks`, `setSlippageBps`, `sweepToken`, `transferOwnership`). (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — `onlyOwner` modifier (:140-143) applied to every listed function; Priority: HIGH; Sources: ADV-12)
- [ ] **GL-23** — An attacker with insufficient allowance can never `withdraw`/`redeem` another holder's shares. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — `_spendAllowance` is invoked whenever `msg.sender != owner_` (:459,:534); Priority: HIGH; Sources: ADV-14)
- [ ] **GL-24** — ETH force-sent to the vault never affects `totalAssets()` or the share price. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — `_totalVaultValueInToken0` (:1048-1086) never reads `address(this).balance`; Priority: LOW; Sources: ADV-20)
- [ ] **GL-25** — `vault.transfer` conserves `totalSupply()`, debits the sender's balance, and credits the recipient's balance by exactly the transferred value. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — OZ `ERC20.transfer` is unmodified by the vault; Priority: LOW; Sources: ADV-22)
- [ ] **GL-26** — `totalAssets() >= sum(convertToAssets(balanceOf(h)))` over `{actors, admin, 0xdead}` (share price never over-promises solvency). (Category: HIGH_LEVEL — solvency; Guarantee: SHOULD-HOLD — `convertToAssets` uses `Math.mulDiv(..., Floor)` over the full supply, which cannot exceed `totalAssets()` in aggregate; Priority: HIGH; Sources: SPEC-01)
- [ ] **GL-27** — `asset() == address(token0)` always, and the call never reverts. (Category: VALID_STATE; Guarantee: SHOULD-HOLD — `asset()` returns the immutable `_s().token0` (:288-290); Priority: LOW; Sources: SPEC-07)
- [ ] **GL-28** — `maxRedeem(holder) == balanceOf(holder)` whenever `!paused()`. (Category: VALID_STATE; Guarantee: SHOULD-HOLD — `maxRedeem` returns `paused ? 0 : balanceOf(owner_)` (:368-370); Priority: MEDIUM; Sources: SPEC-10)
- [ ] **GL-29** — `convertToShares(0) == 0`, `convertToAssets(0) == 0`, and `deposit(0, receiver)` always reverts with `ZeroAmount`. (Category: VALID_STATE; Guarantee: SHOULD-HOLD — `Math.mulDiv(0, ...) == 0` identity; explicit guard `if (assets == 0) revert ZeroAmount()` (:382); Priority: LOW; Sources: RD-04)
- [ ] **GL-30** — `convertToShares`/`convertToAssets` are monotonically non-decreasing in their input. (Category: VARIABLE_TRANSITION; Guarantee: SHOULD-HOLD — `Math.mulDiv` with `Floor` rounding is monotone non-decreasing in the numerator; Priority: MEDIUM; Sources: RD-07)
- [ ] **GL-31** — `VaultMath.token1ToToken0(token0ToToken1(x)) <= x` and `VaultMath.token0ToToken1(token1ToToken0(y)) <= y` at the pool's current TWAP `sqrtPrice`. (Category: VARIABLE_TRANSITION; Guarantee: SHOULD-HOLD — nested `Math.mulDiv(..., Floor)` composition in `VaultMath` (:22-43); Priority: MEDIUM; Sources: RD-08)

## Specific Properties
> Operation-gated postconditions. Implemented as `internal function property_*()` in `Properties.sol` and called from the relevant handler after `snapshotAfter()`.

- [ ] **SP-01** — Dedicated round-trip handler: `deposit(assets)` then (after rolling +1 block) `withdraw(maxWithdraw(...))` must NOT increase the actor's `token0` balance versus before the deposit. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — `deposit` floors minted shares while `previewWithdraw` ceils burned shares (:296-304,325-333,357-366), both favoring the protocol; the mock router swaps 1:1 so no swap noise masks the identity; After: dedicated `property_RT01_depositWithdrawRoundTrip` handler; Priority: HIGH; Sources: RT-01 — **MANDATORY C2 property, non-mergeable, do not drop**)
- [ ] **SP-02** — Dedicated round-trip handler: `deposit(assets)` then (after rolling +1 block) `redeem(balanceOf(actor))` — the actor's combined value out (token0 received + token1 received valued at TWAP) must NOT exceed the token0 originally deposited. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — `previewRedeem`/`convertToAssets` and all of `redeem`'s internal shares (`freed0/1`, `idleShare0/1`) use `Math.Rounding.Floor` (:570-586); After: dedicated `property_RT02_depositRedeemRoundTrip` handler; Priority: HIGH; Sources: RT-02 — **MANDATORY C2 two-leg property, non-mergeable, do not drop**)
- [ ] **SP-03** — Dedicated round-trip handler: `mint(shares)` then (after rolling +1 block) `redeem(shares)` — recovered combined value (token0 + token1@TWAP) must NOT exceed the token0 paid for the mint. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — `previewMint` ceils (:347) while `previewRedeem` floors; After: dedicated `property_RT03_mintRedeemRoundTrip` handler; Priority: MEDIUM; Sources: RT-03)
- [ ] **SP-04** — Dedicated round-trip handler: `depositToken1(amt)` then (after rolling +1 block) `redeem(balanceOf(actor))` — combined value out, converted to token1 terms, must NOT exceed the token1 amount originally deposited. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — triple-floor composition: `token1ToToken0` floor + share-mint `mulDiv` floor + redeem floors; After: dedicated `property_RT04_depositToken1RedeemRoundTrip` handler; Priority: MEDIUM; Sources: RT-04)
- [ ] **SP-05** — Splitting a withdrawal into `withdraw(a)` then `withdraw(b)` burns `>=` the shares that a single `withdraw(a+b)` would have burned (per `previewWithdraw`). (Category: HIGH_LEVEL; Guarantee: EXPLORATORY — ceiling-twice `>=` ceiling-once is plausible but not proven for all splits; After: `rebalancerVaultUpgradeable_withdraw` (paired calls); Priority: LOW; Sources: RT-05)
- [ ] **SP-06** — `previewDeposit(assets)` computed before a `deposit` is `<=` the actual shares minted, for a non-empty vault. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — ERC-4626 `previewDeposit` "no more than the exact amount" clause; both `previewDeposit` and `deposit`'s minting path use the identical Floor `mulDiv` (:332,401-406); After: `rebalancerVaultUpgradeable_deposit`; Priority: MEDIUM; Sources: RD-01)
- [ ] **SP-07** — `previewWithdraw(assets)` computed before a `withdraw` is `>=` the actual shares burned. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — ERC-4626 `previewWithdraw` "no fewer than the exact amount" clause; `withdraw` sets `shares = previewWithdraw(assets)` directly (:456,491); After: `rebalancerVaultUpgradeable_withdraw`; Priority: MEDIUM; Sources: RD-02)
- [-] **SP-08** — Empty-vault divergence: `convertToShares(assets) == assets` but `previewDeposit(assets) == assets - DEAD_SHARES`, a gap of exactly `DEAD_SHARES`. (Category: VARIABLE_TRANSITION; Guarantee: SHOULD-HOLD — direct comparison of `:301` vs `:330`; **VACUOUS under this harness** — `Base.setup()` always seeds the vault with a first deposit, so `totalSupply()` is never 0 during fuzzing unless a full-drain path is added; kept as a spec note, `/fizz-convert` should skip; Priority: LOW; Sources: RD-05)
- [ ] **SP-09** — After a successful `rebalance()`: `tokenId()` is nonzero, `tokenId() != oldTokenId`, and `rebalanceCount() == oldRebalanceCount + 1`. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — burn-then-mint sequence (`_burnPosition` at :862, fresh mint at :906) guarded by `NoLiquidityMinted` (:904), single `rebalanceCount++` site (:907); After: `rebalancerVaultUpgradeable_rebalance`; Priority: HIGH; Sources: ST-02, SPEC-09, CON-05)
- [ ] **SP-10** — `transferOwnership(newOwner)` sets `pendingOwner() == newOwner` and leaves `owner()` unchanged; `acceptOwnership()` (called only by `pendingOwner`) sets `owner() == oldPendingOwner` and resets `pendingOwner() == address(0)`. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — explicit assignments at `:913-919` (propose) and `:921-927` (accept); **NOTE: likely vacuous in this harness** — `owner() == operator() == guardian() == address(this)` and owner-setters are not wired into any actor-pranked handler, so this transition may never fire under the current handler set; kept for completeness; After: would need a dedicated ownership-transfer handler (not currently wired) — implementers may add one or leave as a manual/asserted-only check; Priority: LOW; Sources: ST-03, ST-04, VS-01)
- [ ] **SP-11** — `applyPerformanceFee()` copies `pendingFeeBps → performanceFeeBps` and `pendingFeeRecipient → feeRecipient`, and does NOT clear `pendingFeeBps`/`pendingFeeRecipient` afterward (asymmetric vs. the ownership propose/accept pattern). (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — explicit copy at `:981-982`; no clear-statement exists in `applyPerformanceFee` (:978-984); After: `_rebalancerVaultUpgradeable_applyPerformanceFee`; Priority: MEDIUM; Sources: ST-06, VS-05)
- [ ] **SP-12** — `withdraw`/`redeem` revert with `SameBlock` whenever `block.number <= ghost_lastDepositBlock[owner_]`. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — explicit guards at `:450` and `:528`; **requires ghost mirror** `ghost_lastDepositBlock[address]` since `lastDepositBlock` is a private mapping with no getter — the ghost must be updated in the deposit/mint/depositToken1 handlers to shadow `:388,:425,:636`; After: `rebalancerVaultUpgradeable_withdraw`, `rebalancerVaultUpgradeable_redeem`; Priority: HIGH; Sources: ST-07)
- [ ] **SP-13** — Every liquidity/swap operation (`initializePosition`, `collectFees`, `rebalance`'s decrease/swap/mint, `_removeProportionalLiquidity`) is called with `deadline == block.timestamp + 300`. (Category: VARIABLE_TRANSITION; Guarantee: SHOULD-HOLD — literal deadline computation at each call site (`:697,728,825,900,1142,1172`); **implementation deferred** — requires mock instrumentation (a deadline-recording spy in `MockPositionManager`/`MockCLSwapRouter`) not present in the current harness; left as a TODO stub; Priority: LOW; Sources: ST-08)
- [ ] **SP-14** — `setPaused(v)` sets `paused() == v` (owner-only); `pauseByGuardian()` sets `paused() == true` and is callable only by `guardian()`; a non-owner `guardian` can never `setPaused(false)` to unpause. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — explicit assignments `:935-945`, `onlyOwner` on `setPaused` (:935); **NOTE: the guardian-vs-owner distinction is likely vacuous in this harness** since `owner() == guardian() == address(this)`; kept for completeness; After: `_rebalancerVaultUpgradeable_setPaused`; Priority: MEDIUM; Sources: ST-10, ADV-13)
- [ ] **SP-15** — `proposePerformanceFee(bps, recipient)` atomically sets `feeChangeActiveAt() == block.timestamp + 2 days`, `pendingFeeBps() == bps`, and `pendingFeeRecipient() == recipient`. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — explicit assignments at `:971-975`; After: `_rebalancerVaultUpgradeable_proposePerformanceFee`; Priority: MEDIUM; Sources: VS-03)
- [ ] **SP-16** — Dedicated handler: `deposit` then `donateToken0` then (after rolling +1 block) `redeem` — the donor's combined value after must NOT exceed the value before (a self-donor cannot profit from donating to themselves). (Category: HIGH_LEVEL; Guarantee: EXPLORATORY — inferred anti-manipulation property, not a cited identity; both legs valued at TWAP; After: dedicated `property_ADV04_selfDonationNoProfit` handler; Priority: HIGH; Sources: ADV-04)
- [ ] **SP-17** — A donation only redistributes value pro-rata among existing holders: `previewRedeem(shares)` after a donation is `<= previewRedeem(shares)` before the donation `+ donatedToken0Equivalent`. (Category: HIGH_LEVEL; Guarantee: EXPLORATORY — inferred bound, not an exact cited identity; After: `rebalancerVaultUpgradeable_donateToken0`/`donateToken1`; Priority: MEDIUM; Sources: ADV-05)
- [ ] **SP-18** — When `totalSupply() == DEAD_SHARES` (all non-dead holders have fully exited), a fresh `deposit(assets > DEAD_SHARES, receiver)` still succeeds (the vault is never permanently bricked). (Category: VALID_STATE — guards the "fully drained" state; Guarantee: SHOULD-HOLD — the only bricking condition is `NoAssets` on `totalValBefore == 0` (:400), which this property confirms does not block the drained-but-reseedable path; After: `rebalancerVaultUpgradeable_deposit` (triggered when pre-state has `totalSupply()==DEAD_SHARES`); Priority: HIGH; Sources: ADV-08)
- [ ] **SP-19** — Dedicated handler (same-block bypass probe): `deposit` (as depositor) then `vault.transfer` shares to a fresh actor then, in the SAME block, that fresh actor calls `redeem` — this should NOT succeed under the intended same-block-guard design, but currently WILL succeed because `transfer` does not stamp `lastDepositBlock` for the recipient. (Category: STATE_TRANSITION — probes the same-block guard's completeness; Guarantee: EXPLORATORY — this is a known, expected-to-fire harness/audit finding (guard bypass via share transfer), not a confirmed spec violation until triaged; **requires** `ghost_lastDepositBlock` mirror; After: dedicated `property_ADV09_sameBlockBypassViaTransfer` handler; Priority: HIGH; Sources: ADV-09 — expected to fire)
- [ ] **SP-20** — Dedicated handler (same-block DoS probe): a victim holds pre-existing shares; an attacker calls `deposit(x, victim)` in the same block, stamping `lastDepositBlock[victim]`; the victim's same-block `redeem` of their PRE-EXISTING shares should NOT be blocked, but currently WILL revert with `SameBlock` because `deposit` unconditionally overwrites `lastDepositBlock[receiver]` regardless of the receiver's prior share history. (Category: STATE_TRANSITION — probes the same-block guard for griefing potential; Guarantee: EXPLORATORY — known, expected-to-fire harness/audit finding (donation-triggered DoS); **requires** `ghost_lastDepositBlock` mirror; After: dedicated `property_ADV10_sameBlockDosViaAttackerDeposit` handler; Priority: HIGH; Sources: ADV-10 — expected to fire)
- [ ] **SP-21** — A dust deposit that computes to 0 shares reverts with `ZeroAmount` AND does not pull any tokens from the sender (atomicity — no partial state change on revert). (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — `if (shares == 0) revert ZeroAmount()` (:409) is checked after `safeTransferFrom` (:393) but EVM reverts unwind the whole transaction, so token balances are provably unchanged; After: `rebalancerVaultUpgradeable_deposit_smallAmount`; Priority: MEDIUM; Sources: ADV-15)
- [ ] **SP-22** — A full `redeem` payout's combined value (token0 received + token1 received valued at TWAP) is `<=` the pro-rata share `Math.mulDiv(totalAssetsBefore, shares, supplyBefore) + epsilon`; loosely, a redeemer is never credited more than their idle-balance-plus-position pro-rata share. (Category: HIGH_LEVEL; Guarantee: EXPLORATORY — probes the E-2 pro-rata-fairness pattern; the pro-rata bound is a plausible design invariant but not an exactly cited identity; supersedes the looser `CON-09` bound (`credited0 <= idleBefore0 + totalAssets()`), which had no tight evidence; After: `rebalancerVaultUpgradeable_redeem`; Priority: HIGH; Sources: ADV-17, CON-09)
- [ ] **SP-23** — After `accrueFees` followed by `collectFees`, a holder's subsequent `withdraw(maxWithdraw(...))` never reverts via an arithmetic underflow panic (`0x11`) in the `swept0 - p0` / `swept1 - p1` computation. (Category: HIGH_LEVEL — liveness; Guarantee: SHOULD-HOLD — the subtraction at `:482-485` is unguarded Solidity 0.8 checked arithmetic, and this property directly probes that the accrue→collect→withdraw sequence cannot trigger it; After: `rebalancerVaultUpgradeable_withdraw` (following `accrueFees`+`collectFees` in the same sequence); Priority: MEDIUM; Sources: ADV-19)
- [ ] **SP-24** — `deposit(assets, receiver)` credits shares to `receiver`, not to `msg.sender`, when they differ. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — `_mint(receiver, shares)` (:410); After: `rebalancerVaultUpgradeable_deposit` (with `receiver != actor`); Priority: MEDIUM; Sources: SPEC-03)
- [-] **SP-25** — The first real deposit into a pristine vault (`supply == 0`, `assets > DEAD_SHARES`) yields `shares > 0`. (Category: VALID_STATE; Guarantee: SHOULD-HOLD — direct read of `:395-398,:409`; **VACUOUS under this harness** — `Base.setup()` always seeds the vault before fuzzing begins, so `supply == 0` never occurs during a campaign unless a full-drain path is added; kept as a spec note, `/fizz-convert` should skip; Priority: LOW; Sources: SPEC-06)
- [ ] **SP-26** — `rebalance()` and `collectFees()` never change `totalSupply()`. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — neither function's call graph contains a `_mint`/`_burn` call (all mint sites: `:397,410,436-437,655,668`; all burn sites: `:491,591` — none reachable from rebalance/collectFees); After: `rebalancerVaultUpgradeable_rebalance`, `rebalancerVaultUpgradeable_collectFees`; Priority: HIGH; Sources: SPEC-08)
- [ ] **SP-27** — After any fee-generating operation (`collectFees`, `rebalance`, `withdraw`, `redeem`), the exact identity holds: `Δ(totalFees0Earned) == feeRecipient`'s token0 credit for that call, and likewise for token1; net proceeds to the user always equal `swept - fee` with `fee` exactly the amount added to `totalFeesXEarned`; no exit path (withdraw vs. redeem) can avoid paying its pro-rata performance fee. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — `collectFees` (:707-763): `net = tokensOwed - fee`, `totalFeesXEarned += fee`; `_rebalanceRemoveFeeCollectBurn` (:792-863): same pattern via `_deductPerformanceFee` (:1181-1202); `withdraw` (:482-489) and `redeem` (:559-566) apply the identical `_deductPerformanceFee` + `totalFeesXEarned +=` pattern on the swept-minus-principal fee portion; After: `rebalancerVaultUpgradeable_collectFees`, `rebalancerVaultUpgradeable_rebalance`, `rebalancerVaultUpgradeable_withdraw`, `rebalancerVaultUpgradeable_redeem`; Priority: HIGH; Sources: CON-06, CON-07, CON-08, ADV-21)
- [ ] **SP-28** — The input token is exactly conserved on entry: on `deposit`, `senderPaid == vaultReceived == assets`; on `depositToken1`, `senderDelta == vaultDelta == token1Amount`. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — single `safeTransferFrom` call site for each path (`:393` for deposit, `:641-645` for depositToken1); `MockERC20` has no fee-on-transfer behavior; After: `rebalancerVaultUpgradeable_deposit`, `rebalancerVaultUpgradeable_depositToken1`; Priority: MEDIUM; Sources: CON-11, CON-15)
